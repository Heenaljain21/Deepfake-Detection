lr: 0.0001
batch_size: 4
num_epochs: 1

train_paths:
  - "C:/Datasets/1000_videos/train"

val_paths:
  - "C:/Datasets/1000_videos/validation"

early_stopping_patience: 3
scheduler_factor: 0.5
scheduler_patience: 2
monitor_metric: val_loss

log_every_n_steps: 1
save_top_k: 1